#include <stdio.h>

int main(void) 
{
  float km,valordias,valorkm,r3,valortotal;
  printf("Quantidade de km que sera percorrido? ");
  scanf("%f",&km);

  printf("Quantidade de dias? ");
  scanf("%f",&dias);

  valordias=dias*60;
  valorkm=km*0.15;
  valortotal=r1+r2;

  printf("Preço total %.2f \n",valortotal);

  return 0;
}